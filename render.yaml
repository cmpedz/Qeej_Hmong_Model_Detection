services:
  - type: web
    name: mkqh-guide
    runtime: python
    rootDir: web
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
